"""Optional text normalization utilities.

This module is reserved for Stage 8.
"""


def normalize_noisy_text(text: str) -> str:
    """Return text unchanged until normalization rules are defined."""
    return text
